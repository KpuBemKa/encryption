#include <iostream>
#include <string.h>
#include <bitset>
#include "caesar.h"
#include "vgn.h"
#include "caesarKey.h"
#include "caesarMath.h"
#include "vgm.h"
#include "VGNHash.h"

int main(/* int argc, char *argv[] */)
{
    std::cout << VGNHashEncrypt("nupotveni") << "\n";

    /* std::string decription = argv[1];
    std::string word = argv[2];

    if (decription == "-Ecsr")
    {
        // criptarea cu cesar
        std::cout << CaesarEncrypt(word);
    }
    else if (decription == "-Dcsr")
    {
        // decriptarea cu cesar
        std::cout << CaesarDecrypt(word);
    }
    else if (decription == "-Ecsrm")
    {
        // criptarea cu cesar matematic
        std::cout << CaesarMathEncrypt(word);
    }
    else if (decription == "-Dcsrm")
    {
        // decriptarea cu cesar matematic
        std::cout << CaesarMathDecrypt(word);
    }
    else if (decription == "-Evgn")
    {
        // criptarea cu vigenere
        std::cout << VGNEncrypt(word);
    }
    else if (decription == "-Dvgn")
    {
        // decriptarea cu vigenere
        std::cout << VGNDecrypt(word);
    }
    else if (decription == "-Ecsrk")
    {
        // criptarea cu cesar cu cheie
        std::cout << CaesarKeyEncrypt(word);
    }
    else if (decription == "-Dcsrk")
    {
        // decriptarea cu cesar de cheie
        std::cout << CaesarKeyDecrypt(word);
    }
    else if (decription == "-Evgm")
    {
        // criptarea cu vigenere modificat
        std::cout << VGMEncrypt(word);
    }
    else if (decription == "-Dvgm")
    {
        // decriptarea cu vigenere modificat
        std::cout << VGMDecrypt(word);
    }
 */

    return 0;
}